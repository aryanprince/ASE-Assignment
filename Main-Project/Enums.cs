namespace ASE_Assignment
{
    public enum Action
    {
        rectangle,
        square,
        circle,
        triangle,
        drawto, // This enum is for a line (Issue #34 to update this)
        move,
        reset,
        clear,
        fill,
        pen,
        run,
        var,
        ifstatement,
        endif,
        none,
    }
}
